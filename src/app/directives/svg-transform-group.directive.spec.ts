import { SvgTransformGroupDirective } from "./svg-transform-group.directive";

describe("SvgTransformGroupDirective", () => {
  it("should create an instance", () => {
    const directive = new SvgTransformGroupDirective();
    expect(directive).toBeTruthy();
  });
});
